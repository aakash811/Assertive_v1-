import { test, expect } from "@playwright/test";

test("auth.login.success", async () => {
  expect(1 + 1).toBe(2);
});

test("auth.login.failure", async () => {
  expect(1 + 1).toBe(2);
});
